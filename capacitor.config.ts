import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.distribuidora.app',
  appName: 'Distribuidora',
  webDir: 'dist/distribuidora-frontend/browser'
};

export default config;
